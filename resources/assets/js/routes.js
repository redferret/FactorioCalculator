
import { RootElement } from './bootstrap.js';

export const ROOT                   = RootElement.getAttribute('url');
export const BALANCE_PRODUCTION     = ROOT + '/balance';
export const GET_FACTORIES          = ROOT + '/factories';
